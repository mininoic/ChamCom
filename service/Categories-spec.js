describe('Categories', function() {

  beforeEach(module('ChamCom'));

  it('should ...', inject(function(Categories) {

	//expect(Categories.doSomething()).toEqual('something');

  }));

});